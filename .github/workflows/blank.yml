name: hello-world
on: 
  push:
    branches:
      - main
      - develop
      - 'release/**'
      - feature/**
  pull_request:
    types: opened
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build-step
        run: echo "demo build"
  

