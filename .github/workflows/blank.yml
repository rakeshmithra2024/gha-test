name: hello-world
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build-step
        run: echo "hello ${{ vars.MY_NAME }}"
        run: echo "hello ${{ secrets.DOCKER_PAT }}"

