name: hello-world
on: workflow_dispatch
jobs:
  helloworld-test:
   runs-on: ubuntu-latest
   steps:
     - name: hello-printstep
       run: |
        echo "welcome to GHA1"
        echo "welcome to GHA2"
  

